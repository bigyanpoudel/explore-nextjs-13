/**
 * This tells that this page should not be rendered in server
 * It's a client component
 */
"use client";

const AboutError = () => {
  return <div>error occured in about</div>;
};

export default AboutError;
